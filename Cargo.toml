[package]
name = "competitive"
version = "0.1.0"
edition = "2021"
autobins = false

[dependencies]

# --- BINARIES (one per problem). Do NOT edit manually: it is generated by gen-cargo.ps1 ---
[[bin]]
name = "coin_piles"
path = "problems/coin_piles.rs"

